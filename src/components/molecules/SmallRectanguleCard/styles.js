import styled from 'styled-components'

import theme from '../../../styles/theme'

export const IconWrapper = styled.div`
  display: flex;
  place-content: center;
  width: 22px;
  height: 22px;
  border-radius: 50%;
  background-color: ${theme.yellowTheme};
`
